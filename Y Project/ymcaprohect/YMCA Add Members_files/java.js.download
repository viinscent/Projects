/*global document: false*/

// Creates the function to convert USD TO VND


function usdToVND(usd) {
    // Convert united states dolloars to vietnamese dong
    'use strict';
    return (usd * 23575.00);
}
// Function to handle the conversion from miles to kilometers Creates the
function convert() {

    // Get the USD that the user enters into the input
    'use strict';
    var input = document.getElementById("input").value,
    // Converts what the user inputed for USD into VND
        result = usdToVND(input);
    // Will show the VND ammount in the results
    document.getElementById("result").innerHTML = result + " VND";
}

// Creates a function that lets the user change the color of the button from white to orange.
function colorOrange() {
    'use strict';
    // Changes the color to orange after being clicked.
    document.getElementById("buttonColor").style.backgroundColor = 'orange';
    // The default color.
    document.getElementById("buttonColor").style.color = 'white';
}
//Hiep Nguyen
//February, 12 2023
//5:00 PM
//java.js